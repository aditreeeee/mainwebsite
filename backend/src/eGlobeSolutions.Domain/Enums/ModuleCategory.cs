namespace eGlobeSolutions.Domain.Enums;

/// <summary>Groups a calculator module for display: the core comparison-table
/// modules (PMS, Channel Manager, ...) vs. the additional standalone products
/// (Booking Engine, Website Builder, ...).</summary>
public enum ModuleCategory
{
    CoreModule = 0,
    AdditionalProduct = 10
}
