namespace eGlobeSolutions.Domain.Enums;

/// <summary>The three pricing models offered on the price calculator.</summary>
public enum CalculatorPlanType
{
    PerRoom = 0,
    PerProperty = 10,
    Enterprise = 20
}
